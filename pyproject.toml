[project]
name = "pricing-automation"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "pyyaml>=6.0",
  "pandas>=2.2",
  "numpy>=2.0",
  "openpyxl>=3.1",
  "matplotlib>=3.8",
  "python-pptx>=1.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
